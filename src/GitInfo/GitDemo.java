package GitInfo;

public class GitDemo {

/**
	 * 	What is Git?
	 *  Git is a distributed version control system- Meaning Each developer has full copy of 
	 *  repository.
	 *  It allow multiple user to work simultaneously. 
	 *  
	 *  Git vs SVN-
	 *  SVN is centralized version control system, where single central repo required.
	 *  Git allows developer to work offline, where as SVN requires constant access to the central repo.
	 *  
	 *  Repository-
	 *  Git repository is where all the project files and version history are stored.
	 *  
	 *  Initialization-
	 *  git init 
	 *  git clone- copy of repo in local machine
	 *  git add .
	 *  git status
	 *  git commit -m "messge"
	 *  git push
	 *  
	 *  How to Resolve merge conflict-
	 *  ---Git will mark(red colour) the conflict in the files
	 *  ---Edit the files to fix the conflict (you want to accept or reject)
	 *  ---After resolving the conflict, stages the changes.
	 *  git add .
	 *  git commit -m "msg"
	 *  
	 *  
	 *  What does git stash do?
	 *  Temporarily saves changes in our working directory that are not yet commited
	 *  You can later retrieve them using cmd-
	 *  git stash pop
	 *   
	 *  
	 *  Creating new Branch in git
	 *  git branch branchnm
	 *  
	 *  checkout/ changing the branch name
	 *  git checkout branchname
	 *  
	 *  changing the branch name
	 *  git checkout branchnm
	 *  
	 *  Rename current branch
	 *  git branch -m newbranchname
	 *  
	 *  git push --force
	 *  git reset
	 *  
	 *  Show the graph history of changes
	 *  git log --graph
	 *  
	 *  git log
	 *  
	 *  gitignore- 
	 *  
	 *  
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
 */

}
